# تعليمات رفع تقارير v1.0.6

## الخيار الموصى به للتحديث الحالي

بما أن المستودع يعمل على `v1.0.5`، استخدم حزمة **changed_files_only** وارفع محتوياتها مع الحفاظ على المسارات والاستبدال.

## النسخة الكاملة

استخدم **full_clean_backup** للاحتفاظ بنسخة كاملة نظيفة أو لاستبدال محتوى المستودع كاملًا عند الحاجة.

## Supabase

بعد رفع الواجهة، استبدل كود وظيفة `analyze-educational-form` بمحتوى `SUPABASE_FUNCTION_GEMINI_V0_13_6.txt` ثم أعد النشر.

لا يلزم SQL ولا سر جديد.

## اختبار القبول

ارفع ملف Word التجميعي نفسه، ثم تحقق من ظهور المدرسة والمادة والصفوف والعام الدراسي في التقرير، ومن وصف العبارات المتباينة بأنها تباين سياقي يحتاج فصل السجلات عند غياب معرف المعلم أو الزيارة.

## الملفات المخفية

المسار الحقيقي `.github/workflows/deploy.yml` وملف `.nojekyll` موجودان. توجد نسخة مرئية احتياطية داخل `GITHUB_WORKFLOW_VISIBLE` و`NOJEKYLL_VISIBLE.txt` للاستخدام عند الرفع من الهاتف فقط.
