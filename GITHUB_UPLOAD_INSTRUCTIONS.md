# تعليمات رفع تقارير v1.1.1

## الخيار الموصى به

بما أن المستودع يعمل على `v1.0.6`، استخدم حزمة **changed_files_only** وارفع محتوياتها مع الحفاظ على المسارات والاستبدال.

## النسخة الكاملة

استخدم **full_clean_backup** للاحتفاظ بنسخة كاملة نظيفة أو لاستبدال محتوى المستودع كاملًا عند الحاجة.

## Supabase

بعد رفع الواجهة، استبدل كود وظيفة `analyze-educational-form` بمحتوى `SUPABASE_FUNCTION_GEMINI_V0_14_0.txt` ثم أعد النشر.

لا يلزم SQL ولا سر جديد.

## اختبار القبول

ارفع ملف PDF المكوّن من 14 صفحة و7 زيارات، ثم تحقق من:

- ظهور النوع `زيارات إشرافية متعددة`.
- فصل 7 زيارات.
- استخراج 91 تقديرًا رقميًا.
- فهم أن المستوى 1 هو الأفضل و5 يحتاج إلى تدخل.
- عدم خلط النصوص أو المعلمين بين الزيارات.
- استخراج المدرسة والعام الدراسي والصفوف.

## الملفات المخفية

المسار الحقيقي `.github/workflows/deploy.yml` وملف `.nojekyll` موجودان. توجد نسخة مرئية احتياطية داخل `GITHUB_WORKFLOW_VISIBLE` و`NOJEKYLL_VISIBLE.txt` للاستخدام عند الرفع من الهاتف فقط.
