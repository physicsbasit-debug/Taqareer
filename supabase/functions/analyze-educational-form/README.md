# analyze-educational-form — v0.10.0

وظيفة Supabase Edge لتقارير v0.9.5.

## العمليات

- `ping`
- `classify`
- `vision_extract`
- `analyze` للتوافق
- `enrich` للتوافق
- `enrich_segment` للمسار التشغيلي الحالي

## بروتوكول التشغيل

- بروتوكول الأجزاء: `4.2.0`.
- محرك مهام الجودة الدقيقة: `1.0.0`.
- `quality-tool` يستقبل أداة جودة واحدة فقط، بحد إخراج 520 رمزًا ومحاولة مركزة 320 رمزًا.
- لا يُرسل `responseJsonSchema` معقد في مسار التحسين العميق.
- التحقق من المعرفات والحقول والأدلة يتم خادميًا.
