# analyze-educational-form

وظيفة Supabase Edge لتطبيق تقارير.

العمليات الحالية:

- `ping`
- `classify`
- `vision_extract`
- `analyze_primary`
- `enhance_fast` للتوافق مع العملاء الأقدم فقط

## analyze_primary v0.14.2

- عقد `6.6.0`.
- 3 محاور تحليلية في المسار الطبيعي، و2 في الإنقاذ.
- فصل التشخيص عن الاستنتاج القراري.
- 2–3 تدخلات متمايزة و3 مراحل متابعة بالضبط.
- `thinkingLevel: low` مع إنقاذ واحد بـFlash-Lite و`minimal`.
- كل الادعاءات مرتبطة بمراجع أدلة مسموحة.
- ملفات الدرجات ترسل `targetGroupIds` و`successMetric` منظمين.
- الخادم يعيد بناء `targetGroup` و`successIndicator` من الأعداد المحلية.
- الأهداف المستحيلة تُخفض إلى القدرة الفعلية للفئات المستهدفة، مع تسجيل `numericGuard`.
- العميل يعيد التحقق من `numericGuard` قبل عرض التقرير.

- يستخدم بيانات الترويسة المنظمة عند توفرها.
- يحمي سياق التقارير التجميعية: التباين بين الزيارات أو المعلمين لا يُسمى تناقضًا مؤكدًا دون دليل على وحدة الحالة والزمن.

## Phase 2-C multi-visit supervision

- `recognizedType.id = supervision_multi_visit` يستقبل `data.visits` كسجلات مستقلة.
- المقياس معكوس: `1 متميز` و`5 يحتاج إلى تدخل`.
- لا يرتب المعلمين ترتيبًا شخصيًا ولا يخلط ملاحظات الزيارات.
- عدم الاتساق الرقمي والسردي يُفحص داخل الزيارة نفسها فقط.
- أسماء المعلمين وأرقام الملفات تكون محجوبة افتراضيًا قبل وصولها إلى الوظيفة.

## V0.14.2 - transient capacity fallback

- إعادة مؤقتة بتراجع أُسّي وjitter لأخطاء `408/429/5xx`.
- التحول بين 3.5 Flash و3.6 Flash و3.5 Flash-Lite عند استمرار الازدحام.
- رسائل عربية آمنة للمستخدم دون تمرير النص الخام من Gemini.

## V0.14.2 - coordinated deadline and fast failover
- مهلة خادم كلية 72 ثانية للتحليل الأساسي.
- مهلة 30 ثانية لكل نموذج أساسي و24 ثانية لمسار الإنقاذ.
- محاولة واحدة لكل نموذج ثم انتقال مباشر للنموذج البديل.
- نافذة العميل 85 ثانية حتى يعيد الخادم رسالة عربية قبل إلغاء المتصفح للطلب.
