# v1.4.2 — Workspace Visual Polish

- إعادة تخطيط تبويب «إعداد التحليل» إلى تدفق واحد بعرض منطقة العمل بدل شبكة نصفية تترك مساحة فارغة عندما تكون بطاقة القياس هي العنصر الوحيد.
- أصبحت إعدادات القياس أربعة حقول متوازنة على الشاشات الواسعة، ثم عمودين/عمود واحد استجابيًا دون تغيير IDs أو منطق الحقول.
- دمج شارة «خطة تحليل متكيفة» مع عنوان الخطوة بدل بقائها منفصلة في طرف الصفحة، وإضافة سطر إرشادي قصير يوضح التسلسل.
- تحويل `setupMessage` في الحالة العادية من تنبيه كهرماني إلى Status هادئ أزرق/تركوازي مع مؤشر نشاط؛ الأخطاء تبقى حمراء كما هي.
- تمييز بطاقات الخطة والأدلة وخدمة التحليل بصريًا مع المحافظة على نفس المحتوى والربط، وتحويل ملاحظات القياس والخصوصية إلى Info Notes غير تحذيرية.
- تحسين حالات التركيز للأزرار والحقول وتوازن بطاقات صيغ الإدخال، مع احترام `prefers-reduced-motion`.
- لا تغيير في PDF Intake أو الحسابات أو AI/Edge أو الحارس التربوي أو Print Report V2. Supabase Edge تبقى `V0.16.4`.

# v1.4.1 — Assessment Component Contract Fix

- أصلح استخراج `analyzedGrade` عندما يأتي الصف في سطر PDF مركب مثل `الصف: التاسع - كل الشعب`، مع بقاء `schoolGradeRange=8-10` مستقلاً.
- أضيف عقد خادمي خاص بـ`assessment_component` يملك فئات التدخل من الحسابات المحلية بدل ربط رقم الوحدة التحليلية برقم الفئة: الفجوة العميقة لها تدخل مستقل، والمتوسطة + القريبون من الإتقان لهما مسار رفع متدرج، والمتقنون مسار تثبيت عند الحاجة.
- أصبح `targetGroup → action → successIndicator` عقدًا متماسكًا؛ لا يسمح لتدخل عن القريبين من الإتقان أن يقيس فجوة عميقة أو العكس.
- أضيف حارس عميل نهائي يعيد فحص الدافعية/الثقة/القلق والمشاركة قبل التقرير حتى لا تعتمد الواجهة على Edge وحدها في منع الاستدلال النفسي غير المقاس.
- أضيفت اختبارات End-to-End على توزيع المكوّن الحي 268 طالبًا: 141 متقنًا، 47 قريبًا، 71 فجوة متوسطة، و9 فجوة عميقة.
- التطبيق `v1.4.1` وSupabase Edge `V0.16.4`. لا تغيير في Print Report V2 أو الحسابات أو ترتيب الأوائل أو استعادة الأسماء.

# v1.4.0 — Print Report V2 Production

- إضافة Renderer إنتاجي جديد `assets/print-report-v2.js` مبني على الصفحة A4 كوحدة تصميم، بدل محاولة تحويل شبكة بطاقات الواجهة إلى ورق بعد اكتمالها.
- Print Report V2 يستهلك `TaqareerReports.buildReportData()` نفسه، لذلك لا يعيد حساب المؤشرات ولا يمس ترتيب الأوائل أو الأسماء أو الاستدلال التربوي.
- التقرير الجديد هو المسار الافتراضي لزر التقرير التنفيذي والكامل، مع بقاء Renderer القديم كـfallback تلقائي إذا تعذر بناء V2.
- التقرير التنفيذي يستهدف 3–5 صفحات؛ التقرير الكامل يجمع المؤشرات والقراءات وخريطة القرار وخطة التحسين والمتابعة، ثم يعبئ جداول الأوائل 2–3 مواد في الصفحة حسب حجم الجداول.
- إضافة حارس Runtime مستقل يفحص تجاوز A4 ويحفظ أعداد التشخيصات والاستنتاجات والتدخلات والمتابعة وجداول الترتيب.
- Supabase Edge تبقى `V0.16.3` بلا تغيير؛ لا SQL ولا أسرار ولا إعادة فهرسة.

# v1.3.4 — Adaptive A4 Pagination & Density

- إصلاح سبب الانقسام المفرط: الشبكة التي لا تتسع في المساحة المتبقية تُجرّب كاملة أولًا على صفحة A4 جديدة قبل تفكيكها إلى بطاقات.
- إضافة تخطيط تدفق عمودي كامل العرض للقراءة التفسيرية والاستنتاجات وخطط التدخل والمتابعة والحوكمة عندما يكون العمودان أضعف كفاءة بسبب التفاف النص.
- ضغط المسافات والحشوات فقط داخل صفحات التدفق المقاسة دون تصغير أحجام الخطوط أو حذف أي نص.
- إضافة حارس يحصي بطاقات التشخيص والاستنتاج والتدخل والأدوات والمتابعة والحوكمة قبل إعادة التوزيع وبعدها لمنع فقد أو تكرار المحتوى.
- لا تغيير في PDF Intake أو الحسابات أو الأسماء أو الترتيب أو الاستدلال التربوي أو مسار AI. Supabase Edge تبقى V0.16.3 ولا تحتاج إعادة نشر.

# v1.3.3 — End-to-End Inference Taint Guard

- وسّعت حراسة الاستدلال لتفحص `recommendedAction` مع الحقول التفسيرية قبل التوسيع الخادمي.
- أي فرضية غير مقاسة تحمل `inferenceTaint` حتى التدخل المشتق، وتتحول خطة التعامل معها إلى **تحقق من الدليل قبل التدخل** بدل علاج سبب غير مثبت.
- تصنيف طبيعة المادة «عملية/تطبيقية/نظرية» لا يُعامل كسبب مقاس؛ يعاد إلى تفاوت وصفي ما لم توجد بيانات مستقلة.
- فُصلت «القلق/العامل النفسي/العامل الاجتماعي/العامل الأسري» حتى يبقى اسم الفرضية واضحًا، مع منع اعتبار اسم مادة «الدراسات الاجتماعية» عاملًا اجتماعيًا.
- أضيف وسم رتبة الدليل داخل بطاقة خطة التحسين عند اشتقاقها من فرضية أو استنتاج محروس.
- أصلح إصدار `index.html` وcache-busters القديمة التي بقيت على v1.3.1 رغم أن التطبيق كان v1.3.2.
- التطبيق v1.3.3 وSupabase Edge V0.16.3. لا تغيير في PDF Intake أو الحسابات أو الأسماء أو ترتيب الأوائل.

# v1.3.2 — Educational Inference Guardrails

- أضيف حارس خادمي بعد Decision Core وقبل التقرير لملفات الدرجات، بحيث لا تتحول الفروق الرقمية إلى أسباب نفسية أو اجتماعية أو تدريسية غير مقاسة.
- أي ادعاء عن الثقة الذاتية أو الدافعية أو المشاركة أو القلق أو جودة التدريس أو صعوبة المحتوى/الاختبار من الدرجات وحدها يُخفض إلى `hypothesis` بثقة منخفضة مع حد استدلال وبيانات مطلوبة للتحقق.
- الفروق بين المواد لا تُفسر بسبب كون المادة «تطبيقية/نظرية» ما لم تكن هذه السمة متغيرًا صريحًا في المصدر؛ يعاد التعبير عنها كتفاوت وصفي وترتيب أولوية فقط.
- الملخص التنفيذي يُنظف من الفرضيات النفسية/السببية غير المقاسة.
- التقرير يعرض مستوى العبارة صراحة: «مؤكد من البيانات / استنتاج مدعوم / فرضية تحتاج تحققًا».
- أضيف Regression End-to-End يحقن مخرجات AI غير منضبطة ويتحقق أن الخادم يمنعها قبل عقد التقرير، مع إبقاء الاستنتاج الرقمي الصحيح كما هو.
- التطبيق `v1.3.2` وSupabase Edge `V0.16.2`. لا تغيير في PDF Intake أو الحسابات أو ترتيب الأوائل أو استعادة الأسماء.

# v1.3.1 — Resilient Decision Core v2

- صُغّر عقد Gemini إلى حكم تنفيذي و2–3 وحدات قرار فقط؛ يبني الخادم الملف الكامل حتميًا.
- أصبح `gemini-3.5-flash-lite` هو نموذج القرار الأساسي و`gemini-3.6-flash` fallback واحدًا، مع إعدادات مستقلة `GEMINI_DECISION_MODEL` و`GEMINI_DECISION_FALLBACK_MODELS`.
- عند تعذر المزود أو رفض العقد بعد المحاولتين، لا يتوقف التقرير: يبني الخادم قراءة محلية موثوقة من الحسابات والأدلة ويعلّمها صراحة كـlocal fallback. أخطاء الإعداد والمفتاح تبقى مانعة ولا تُخفى.
- بروتوكول التنسيق الداخلي أصبح `6.8.0` وEdge `V0.16.1`.

# v1.3.0 — Single Decision Core Architecture

- إعادة بناء جذرية لمسار `analyze_primary` بعد فشل v1.2.48 حيًا بالمهلة: أزيلت سلسلة `reasoning + action + repair + segment rescue` من runtime الأساسي بدل تعديل Retry أو الثواني مرة أخرى.
- كل ضغطة ترسل طلب Edge واحدًا فقط. Edge تستدعي Gemini مرة واحدة طبيعيًا، وبحد أقصى مرتين عند الحاجة: نموذج أساسي + fallback واحد متميز. `429` توقف فورًا.
- Gemini ينتج نواة القرار التشخيصية فقط؛ الخادم يبني التدخلات وثلاث مراحل المتابعة ويملك الحسابات ومؤشرات النجاح، مع بقاء عقد التقرير النهائي `6.6.0`.
- ملفات الدرجات لا ترسل صفوف الطلبة الخام إلى التحليل الذكي؛ ترسل المؤشرات والرسوم والأدلة الحتمية فقط، مع حذف `row:*` من مراجع القرار.
- أضيف cache محلي ببصمة الأدلة لمدة 24 ساعة وبحد 6 نتائج، مع `force` لتجاوز cache عند طلب إعادة التحليل صراحة.
- أضيفت اختبارات End-to-End تثبت: 1 Gemini request في المسار الطبيعي، 2 فقط عند fallback، توقف 429 بعد الطلب الأول، عدم وجود repair/rescue في runtime، عدم إرسال raw score rows، وإعادة النتيجة المكررة من cache دون شبكة.
- Supabase Edge أصبح `V0.16.0` ويلزم إعادة نشره. لا SQL ولا Secret جديد.

# v1.2.48 — Bounded Primary Request Lifecycle

- يمنع إعادة `analyze_primary` كاملة تلقائيًا بعد `AI_PRIMARY_TIMEOUT` حتى لا تتضاعف مدة الانتظار أو يستمر طلب سابق في الخادم بالتوازي مع طلب جديد.
- يمنع إعادة الطلب الكامل بعد `GEMINI_CONTRACT_REJECTED` لأن Edge تكون قد استلمت نتيجة وحاولت إصلاح العقد بالفعل.
- يحتفظ بمنع الإعادة الفورية لـ`GEMINI_RATE_LIMIT`.
- يسمح بإعادة واحدة فقط لـ`503/504` عندما تعود بسرعة خلال 12 ثانية؛ أما الاستجابة التي استهلكت وقتًا معتبرًا فتُعاد للمستخدم بدل تشغيل دورة تحليل كاملة جديدة.
- يخفض ميزانية إعادة الطلب الكاملة من 3 إلى محاولتين كحد أعلى، مع بقاء إعادة النقل الشبكي السريع محصورة.

# v1.2.47 — Future Guard Hardening

- مراجعة معمارية لحراس الإصدارات أكدت عدم وجود أرقام `319/11/21` كقيود تشغيلية صلبة في الإنتاج؛ هذه الأرقام بقيت Fixtures/معايير قبول فقط.
- أزيل تثبيت رقم الإصدار الحالي من `verify-static` واختبارات الإصدار الحالية؛ أصبحت العقود تستمد نسخة التطبيق من `package.json` ونسخة Edge من مصدر الوظيفة مباشرة.
- أضيف عقد اتساق لـ`package-lock.json` و`manifest.json` وcache-busters ونسخ الواجهة، مع فحص أن مسار GitHub Pages يشغّل `npm run check` قبل النشر.
- أزيل حارس Runtime الذي كان يقبل `GEMINI_ANALYSIS_MODEL` فقط إذا طابق عائلة `gemini-3`; أصبحت أسماء النماذج وقوائم fallback اختيارية وقابلة للضبط دون تغيير القيم الافتراضية الحالية.
- أضيف Regression Runtime يثبت أن اسم نموذج من عائلة مستقبلية يمر فعليًا من الإعداد إلى طلب Gemini بدل إسقاطه إلى نموذج افتراضي قديم.
- Supabase Edge أصبح `V0.15.9`; يلزم إعادة نشر الوظيفة. لا SQL ولا Secret جديد، ومتغيرات fallback الجديدة اختيارية فقط.

# v1.2.46 — Rate-Aware AI Orchestration

- فحص End-to-End بعد فشل v1.2.45 أثبت ثغرة في إدارة أخطاء Gemini: كان `429/RESOURCE_EXHAUSTED` يُعامل كازدحام عابر يسمح بتبديل النماذج ثم rescue ثم إعادة الطلب الكامل من العميل، ما قد يضاعف ضغط RPM/TPM بدل تخفيفه.
- أصبح Edge يميز `rate_limit` عن `capacity/timeout/network`: عند 429 يوقف model fan-out فورًا ويحافظ على الخطأ كـ `GEMINI_RATE_LIMIT` بدل أن يتحول لاحقًا إلى `GEMINI_TRANSIENT`.
- يحتفظ كل مقطع بأسماء النماذج التي جُرّبت، ويستبعدها من `segment transient rescue`; أضيف `gemini-3.1-flash-lite` كنموذج إنقاذ مستقل بدل إعادة نفس النموذج الذي فشل قبل ثوانٍ.
- العميل لا يعيد طلب `analyze_primary` كاملًا فورًا عند `GEMINI_RATE_LIMIT`; أما 503/504 فمسموح لها بإعادة خادمية واحدة فقط لأن Edge نفسها نفذت fallback/rescue داخل الطلب الأول.
- أضيف تشخيص آمن في استجابة الخطأ: نوع فشل المزود، الحالة، النماذج المجربة و`retryAfterMs` عندما يرسله المزود، دون كشف أسرار.
- Supabase Edge أصبح `V0.15.8`. يلزم إعادة نشر `supabase/functions/analyze-educational-form/index.ts`. لا SQL ولا Secret جديد.

# v1.2.45 — Reserved AI Rescue Budget E2E

- إصلاح End-to-End لمسار `analyze_primary` عند استمرار ازدحام Gemini: كانت المرحلة الأولى قادرة على استهلاك معظم مهلة الخادم البالغة 45 ثانية قبل بدء الإنقاذ الشبكي، فيصل `segment transient rescue` بلا وقت كافٍ.
- قُسمت الميزانية زمنيًا إلى سقف للمرحلة الأولى عند 26 ثانية، وسقف لمرحلة الإنقاذ عند 36.5 ثانية، مع إبقاء 8.5 ثانية محجوزة للإصلاح/التحقق النهائي داخل مهلة 45 ثانية.
- المقطع الناجح لا يعاد؛ الإنقاذ ما زال يعيد المقطع المتعثر فقط. لا تغيير في عقود التحليل أو الحسابات أو PDF Intake أو ترتيب الأوائل.
- أضيف Regression زمني في Runtime يحاكي reasoning ناجحًا وaction يستهلك نماذج بطيئة متتالية: يفشل على V0.15.6 بسبب وصول الإنقاذ بعد استنزاف الميزانية، وينجح على V0.15.7 مع الميزانية المحجوزة.
- Supabase Edge أصبح `V0.15.7`. يلزم إعادة نشر `supabase/functions/analyze-educational-form/index.ts` بعد الدمج. لا SQL ولا Secret جديد.

# v1.2.44 — Student Name Order E2E

- فحص End-to-End على التقرير الحي أثبت أن اكتمال الاسم في v1.2.40 لا يضمن ترتيب أجزائه: بعض صفوف PDF.js كانت تضع القبيلة على band رقم الطالب والاسم الأساسي على band القيد، فتظهر القبيلة قبل الاسم.
- أعيد ترتيب جزأي الهوية دلاليًا عند وجود جزء أساسي طويل/ثلاثي وجزء قصير للقبيلة، بحيث يبقى الاسم الأساسي أولًا وتُلحق القبيلة في النهاية بصرف النظر عن أي band وصل أولًا.
- Regression جديد يعيد الحالة المعكوسة التي كانت تفشل على v1.2.43، ويمر من PDF إلى Dataset ثم Ranking ثم HTML النهائي، مع بقاء اختبار الحالة المقابلة ناجحًا.
- لا تغيير في الدرجات أو قواعد الترتيب والتعادل أو PDF Intake لمادة واحدة أو Gemini/Supabase Edge.

# v1.2.43 — Browser CORS Request Contract Fix

- فحص End-to-End بعد فشل v1.2.42 أثبت أن الطلب الحقيقي يتوقف في CORS preflight قبل وصول `analyze_primary` إلى Edge.
- السبب: العميل أضاف رأس التشخيص `x-taqareer-attempt` في v1.2.41، بينما Edge V0.15.6 لا يعلنه ضمن `Access-Control-Allow-Headers`؛ المتصفح لذلك يحجب الطلب ويعيد `Failed to fetch`/خطأ نقل.
- أزيل الرأس التشخيصي غير الضروري من الطلبات، مع بقاء عداد المحاولات داخل العميل واستمرار backoff والمحاولات الثلاث لأخطاء السعة.
- أضيف Contract Regression يضمن أن كل رأس `x-taqareer-*` يرسله المتصفح موجود في CORS allow-list للـEdge، حتى لا يتكرر الانقطاع الصامت.
- لا تغيير في Edge V0.15.6 أو PDF Intake أو الحسابات أو الترتيب أو استعادة أسماء الطلبة.

# v1.2.42 — Direct Analysis Request Health Authority

- إزالة فحص `health` كحاجز إلزامي قبل `analyze_primary`؛ الطلب الحقيقي أصبح دليل الجاهزية الحاكم لمسار التحليل.
- يبقى فحص `health` متاحًا عند فتح الصفحة وفي «حفظ واختبار» كإشارة تشخيصية فقط، ولا يمنع زر التحليل إذا تعثر منفردًا.
- نجاح `analyze_primary` يحدّث حالة الاتصال إلى `live` مباشرة، بينما تبقى أخطاء الشبكة/الإعداد الفعلية صريحة إذا فشل الطلب الحقيقي نفسه.
- Regression End-to-End يثبت أن تعثر فحص الصحة لا يمنع تحليلًا فعليًا قابلًا للوصول، مع الحفاظ على تعافي 503 المتدرج في v1.2.41.
- لا تغيير في PDF Intake أو الحسابات أو الترتيب أو استعادة هوية الطالب أو Supabase Edge V0.15.6.

# v1.2.41 — Adaptive Transient AI Recovery

- يعيد `analyze_primary` تلقائيًا حتى ثلاث محاولات فقط لأخطاء السعة السريعة، مع backoff متدرج بدل إعادة الطلب فورًا داخل موجة الازدحام نفسها.
- لا يسمح بمحاولة ثالثة إذا استغرقت استجابة 503 الثانية أكثر من 20 ثانية، حتى لا يتحول التعافي إلى انتظار طويل.
- يعيد محاولة واحدة بعد مهلة المتصفح `AbortError` بدل رمي `AI_PRIMARY_TIMEOUT` مباشرة رغم كونه قابلًا للإعادة.
- يرسل `x-taqareer-attempt` لكل محاولة لتسهيل تتبع السلسلة في سجلات Edge.
- لا تغيير في PDF Intake أو الحسابات أو الترتيب أو Gemini Edge Function.

# v1.2.40 — Ranking Identity & Report Metadata Cleanup

- إضافة استعادة End-to-End للاسم الكامل للطالب في PDF متعدد المواد عندما تفصل PDF.js أصل الاسم والقبيلة بين bands متجاورة.
- ربط استعادة الهوية برقم الطالب وهندسة صفه لمنع انتقال القبيلة إلى طالب مجاور.
- جداول ترتيب المدرسة وأوائل المواد تستخدم الاسم الكامل المستعاد دون تغيير الدرجات أو قواعد التعادل أو معادلة الترتيب.
- فصل Metadata في التقرير متعدد المواد إلى «نوع المصدر» و«نطاق التحليل» و«المادة».
- لا تغيير في Supabase Edge V0.15.6 أو الحسابات أو معادلات الترتيب أو PDF Intake الخاص بمادة واحدة.

## v1.2.39 — Raw PDF.js Multi-Band Student Row Recovery

- إصلاح End-to-End لمسار كشف النتائج متعدد المواد عندما يفصل PDF.js صف الطالب إلى band للرقم/الدرجات وband مجاور للاسم/الحالة/المستويات.
- إعادة بناء الصف من عناصر PDF الخام مع دعم اختلاف أصل النص العربي RTL بين الحافة اليسرى واليمنى، بدل اشتراط سطر هندسي واحد.
- الحفاظ على حارس الاكتمال: لا يمر الكشف إلا بعد إثبات تسلسل السجلات كاملًا من 1 إلى العدد المتوقع.
- Regression يجمع انزياح RTL مع اختلاف baseline بين الدرجات والمستويات ويثبت استعادة الصفوف قبل تجاوز بوابة `PDF_TABLE_INCOMPLETE`.
- لا تغيير على PDF Intake الخاص بمادة واحدة، الحسابات، Gemini، Supabase Edge، SQL أو الأسرار.

## v1.2.38 — Browser PDF.js Multi-Subject Header Recovery

- فحص End-to-End بعد فشل v1.2.37 أثبت أن المسار المنشور يحمل الكود الجديد، لكن اكتشاف المواد كان يعتمد على تطابق مراكز أجزاء العنوان العربي المكدس بفارق ضيق، بينما PDF.js قد يزيح أجزاء RTL أفقيًا.
- أعيد بناء اكتشاف أعمدة المواد ليستخدم أزواج `الدرجة/المستوى` كمرساة هندسية ثابتة، ثم يربط أجزاء اسم المادة بأقرب عمود؛ لا يعتمد نجاحه على تطابق كلمتي العنوان رأسيًا في المركز نفسه.
- أضيف مسار lexical أكثر تحمّلًا لتجزئة text items عند اكتشاف سطر الطالب الأول.
- Regression جديد يعيد نفس بوابة الفشل العامة `PDF_TABLE_INCOMPLETE` عند تفتيت رؤوس المواد على نمط PDF.js، ويثبت أن v1.2.38 تستعيد 11 مادة وتمر من `readPdf()` بدل السقوط إلى الحارس العام.
- بقي حارس النقص فعالًا؛ لا يتجاوز الملف إلا بعد استعادة تسلسل السجلات كاملًا. لا تغيير في Edge V0.15.6 أو Gemini أو التقرير أو حسابات المادة الواحدة.

## v1.2.37 — Multi-Subject PDF Intake E2E

- إضافة قارئ PDF متخصص لكشوف النتائج الفردية متعددة المواد قبل حارس الجداول العام، حتى لا يلتقط جدول الملخص الصغير المتكرر بدل مصفوفة درجات الطلبة.
- يستخرج القارئ هوية الطالب وحالة القيد و11 زوجًا من الدرجة/المستوى هندسيًا عبر الصفحات، مع استمرار رفض الملف إذا لم يثبت اكتمال تسلسل السجلات.
- صفحات الملاحظات/الرموز التي لا تحتوي سجلات طلبة تُستبعد من صفحات البيانات بدل عدّها صفحات جدول ناقصة.
- تُحفظ الحالات غير الرقمية مثل `م` و`غ` كسجلات مصدر صحيحة ولا تدخل الحسابات الرقمية؛ فصل تعداد سجلات المصدر عن تعداد السجلات الرقمية القابلة للحساب.
- Regression جديد يمر عبر `readPdf()` نفسه إلى الملف الدلالي والتحليل متعدد المواد، ويثبت أن حذف سجل واحد يعيد `PDF_TABLE_INCOMPLETE` بدل تجاوز الحارس.
- لا تغيير في Edge V0.15.6 أو Gemini أو حسابات المادة الواحدة أو محرك التقرير.

## v1.2.36 — Final Print Polish

- أصلح عرض مصدر PDF في رأس التقرير بصياغة عربية كاملة الاتجاه: `ملف بي دي إف • N صفحة`.
- صحح كشف كثافة صفحة الملخص التنفيذي ليحسب بيانات جميع الرسوم الأساسية، بما فيها histogram وstacked100، بدل الاقتصار على bar.
- عند ارتفاع الحمل البصري للرسوم الأساسية يُفعّل تنسيق `executive-dense-charts` الموجود أصلًا، فيضغط الصفحة ضمن A4 ويمنع ملامسة الرسم للتذييل دون تغيير البيانات أو الحسابات.
- لا تغييرات في PDF Intake أو Gemini/Supabase أو الحسابات أو Semantic Router.

## v1.2.35 — Report Presentation Cleanup

- تنظيف العرض النهائي لمصدر البيانات بحيث لا تظهر أسماء تقنية مثل `جدول PDF منظم` أو قوائم الصفحات الخام داخل رأس التقرير؛ تعرض ملفات PDF بصيغة مختصرة مثل `ملف PDF • 14 صفحة` مع بقاء بيانات المصدر الأصلية محفوظة داخليًا.
- استبدال تسمية دمج الأقسام من صيغة تقنية مثل `الاستنتاجات التشخيصية + 2 أقسام` إلى أسماء أقسام مختصرة ومقروءة مثل `الاستنتاجات / خطة التحسين / المتابعة`.
- لا تغيير في PDF Intake أو الحسابات أو Semantic Router أو Gemini/Supabase.

## v1.2.34 — Retryable Edge Recovery & Cache Coherence

- يعيد العميل تلقائيًا طلب `analyze_primary` مرة واحدة عندما ترجع Edge استجابة HTTP قابلة للإعادة مثل `GEMINI_TRANSIENT` أو `GEMINI_RATE_LIMIT` بدل إجبار المستخدم على الضغط يدويًا.
- تبقى أخطاء النقل لها إعادة المحاولة الحالية، ولا تتغير العقود التحليلية أو حسابات الأدلة.
- توحيد نسخة التطبيق ووسوم cache-busting العامة على `1.2.34` لضمان وصول JavaScript الجديد إلى المتصفح بعد النشر.
- Regression جديد يثبت مسار Health → 503 retryable → إعادة تلقائية → نجاح، وحارس يمنع اختلاف نسخة الأصول العامة عن `package.json`.

## v1.2.33 — Real PDF Analyzed Grade Metadata Recovery

- إصلاح End-to-End لمشكلة عرض نطاق المدرسة `8-10` بدل الصف المحلل `الثامن` في التقارير المستخرجة من PDF الوزاري.
- ثبت التشخيص على الملف الحقيقي: PDF.js قد يفصل قيمة الصف بصريًا عن وسم `الصف` إلى سطر مجاور بسبب اختلاف خط الأساس، مع بقاء القيمة قربه هندسيًا.
- أصبح PDF Intake يستعيد قيمة الصف الصريحة من الجوار الهندسي القريب لوسم `الصف` مع رفض نطاقات المدرسة مثل `8-10` كصف محلل.
- بقي `schoolGradeRange` مستقلًا عن `analyzedGrade` حتى Dataset ورأس التقرير النهائي.
- Regression جديد يمر من عناصر PDF المموضعة إلى CanonicalDocument ثم Dataset ثم التقرير، إضافة إلى سيناريو 14 صفحة و319 سجلًا.
- لا تغيير في Edge Function V0.15.6، ولا SQL، ولا أسرار جديدة.

## v1.2.32 — Segment-Level Transient Rescue

- عالج مسار `analyze_primary` سقوط التحليل كاملًا عندما ينجح أحد مقطعي `reasoning` أو `action` بينما يستنفد المقطع الآخر سلسلة النماذج بسبب `503/429/timeout`.
- يحتفظ Edge بالمقطع الذي نجح ولا يعيده من الصفر، ويشغّل محاولة إنقاذ شبكية معزولة للمقطع المتعثر فقط باستخدام نماذج خفيفة ضمن مهلة الخادم نفسها.
- بقي إصلاح Structured Output/عقد الجودة مسارًا منفصلًا؛ الإنقاذ الشبكي لا يُستخدم لأخطاء العقد غير المؤقتة.
- أضيفت Telemetry واضحة: `transientRescueUsed` و`transientRescuedSegments` و`transientInitialAttemptedModels`، من دون إعادة رسائل مزود الخدمة الخام إلى واجهة النجاح.
- أضيف Regression يثبت حالتي تعثر `reasoning` وحده وتعثر `action` وحده، وأن المقطع السليم لا يعاد طلبه.
- Supabase Edge أصبح `V0.15.6`. لا تغيير في PDF Intake أو الحسابات أو Metadata أو عقد التحليل `6.6.0`.

## v1.2.31 — Metadata Grade Separation

- فصل عرض الصف المحلل فعليًا عن نطاق صفوف المدرسة عند بناء التقرير الرسمي.
- أصبح `analyzedGrade` أعلى أولوية عند توفره، مع بقاء `schoolGradeRange` محفوظًا كبيان مستقل عن المدرسة.
- أضيفت أولوية دفاعية مماثلة في ملف التحليل الدلالي ومعاينة بيانات الترويسة، دون تعديل Canonical PDF Intake أو هندسة الأعمدة أو منطق استعادة الصفوف.
- أضيف Regression يثبت أن تقرير ملف للصف الثامن يعرض «الثامن» في خانة الصف/الفئة حتى لو بقي `grade=8-10` للتوافق الخلفي، مع الاحتفاظ بنطاق المدرسة `8-10`.
- لا تغيير في حسابات الدرجات أو Excel/CSV/Word أو Supabase أو Edge Function.

## v1.2.30 — Semantic PDF Span Recovery

- عالجت طبقة PDF العامة سقوط بعض الصفوف القصيرة في جداول RTL عندما يدمج PDF.js قيم أعمدة متجاورة في span واحد أو يجعل الاسم أقرب هندسيًا إلى عمود التسلسل من عمود الاسم.
- صار الإسقاط الهندسي يوازن بين موضع العنصر ونمط القيمة المتوقع من دور العمود، بدل اختيار أقرب مركز فقط.
- أضيف تفكيك عام للـspans المركبة وفق أدوار الأعمدة: `serial/student` و`score/level` و`enrollment/second_round`، من دون شروط لأسماء مواد أو أنواع تقارير أو أسماء ملفات.
- استمرار القيم متعددة الأسطر يُلحق بالسجل المستعاد الصحيح، ولا يتسرب إلى السجل السابق.
- Regression الكامل يحافظ على 319 سجلًا عبر 14 صفحة، مع أربع صفوف مضغوطة مصطنعة، ودرجة صفر صحيحة، ورمز نتيجة غير رقمي محفوظ كما هو.
- لا تغيير في Excel/CSV/Word أو Supabase أو Edge Function.

## v1.2.29 — Full-Document PDF Geometry & Pagination Contract

- أصلح مدخل PDF سبب توقف جداول النتائج الطويلة عند انتقال رقم التسلسل من خانتين إلى ثلاث خانات: صار ربط الخلايا يعتمد على الممرات الهندسية لعناوين الأعمدة، لا على مسافة الفراغ العامة وحدها.
- يحتفظ `groupPdfItemsIntoLines` بحدود كل خلية ومركزها، وتستخدم طبقة `Canonical PDF Intake` عناصر النص الخام لاستعادة العمود الصحيح عندما تندمج خليتان بصريًا في السطر.
- أضيف عقد اكتمال متعدد الصفحات: إذا تكرر رأس الجدول في صفحات لم تدخل في الجدول الموحّد، يُرفض الجدول كغير مكتمل بدل الرجوع إلى مسار قديم وتحليل جزء منه.
- أضيف تحقق تسلسل للسجلات داخل metadata البنيوية: أول رقم، آخر رقم، عدد القيم الفريدة، التكرارات والفجوات.
- أصبحت بيانات PDF تميز بين نطاق صفوف المدرسة وبين الصف المحلل فعليًا، وتقرأ المادة الصريحة من أزواج label/value مثل `المادة : العلوم`.
- أضيف Regression هندسي من 319 سجلًا موزعًا فعليًا على 14 صفحة، ويعيد إنتاج اندماج رقم التسلسل ذي الثلاث خانات مع خلية الاسم من دون استخدام أسماء طلبة حقيقية.
- لا تغيير في Excel/CSV/Word أو Supabase أو Edge Function.

## v1.2.28 — Canonical RTL Column Alignment

- أضيفت طبقة عامة `assets/pdf-column-alignment.js` بين اكتشاف الجدول وربط الأعمدة، لتثبيت هوية الأعمدة العربية من عنوان العمود ونمط القيم بدل الاعتماد على ترتيب الخلايا وحده.
- أصبحت جداول النتائج الفردية متعددة الصفحات تحتفظ بالأعمدة البنيوية كاملة قبل إسقاط الحقول غير اللازمة للتحليل: التسلسل، الاسم، الجنسية، حالة القيد، الدرجة، المستوى، والملاحظات/الدور الثاني عند وجوده.
- يختبر المحرك اتجاه الخلايا المصدر وعكسه عند PDF RTL، ويختار الربط الأعلى اتساقًا دلاليًا. إذا فشل عمود حرج مثل الاسم أو الدرجة في التحقق، لا يُعتمد الجدول للتحليل.
- أضيف Regression اصطناعي من 319 سجلًا موزعًا على 14 صفحة، يتضمن درجة صفر صحيحة وقيمة غير رقمية محفوظة كما هي، دون تضمين أي بيانات طلاب حقيقية.
- بقيت جداول 13×2، الجداول الهرمية، والسرد الإشرافي ضمن اختبارات عدم الانحدار.

# سجل الإصدارات

## v1.2.27 — Hierarchical PDF Table Structure

- أضيفت طبقة عامة `assets/pdf-table-structure.js` داخل Document Intake لفهم الجداول ذات الرؤوس متعددة المستويات قبل المرور إلى Semantic Router.
- تدعم الطبقة Parent/Child headers والأعمدة الفرعية المتكررة، وتبني إسقاطًا تحليليًا على مستوى العناوين الأب مع الاحتفاظ بتفاصيل الأعمدة الفرعية في metadata البنيوية.
- صفوف النسب المرافقة لا تتحول إلى سجلات مستقلة؛ تحفظ كأدلة بنيوية مرتبطة بصفوفها، بينما يعتمد التحليل العددي على صفوف العدد.
- الإجماليات والجمل العامة المتكررة عبر الصفحات تُزال بالاعتماد على البصمة الرقمية مع تفضيل `grand_total`، لمنع مضاعفة الإجمالي عند تكراره في صفحة لاحقة.
- ارتفع عقد `CanonicalDocument` إلى `2.1.0`، مع بقاء الجداول المسطحة والسرد وShadow/Fallback السابق كما هو.
- أضيف استخراج أكثر صلابة للمادة عندما يكون عنوان التقرير واسم المادة في خليتين منفصلتين داخل السطر نفسه.
- أضيف اختبار عام بجدول هرمي اصطناعي لا يحتوي أسماء مواد أو أنواع تقارير، إضافة إلى Regression لبنية ملف توزيع مستويات متعدد الرؤوس.
- لا تغيير في Excel/CSV/Word أو Supabase أو Edge Function أو عقود التحليل الخارجي.


## v1.2.26 — Adaptive Analysis Setup Semantics

- فصلت صياغة شاشة «إعداد التحليل» في سياسة مستقلة قابلة للاختبار داخل `assets/setup-policy.js` بدل خلطها بمنطق العرض العام.
- أصبحت قائمة «الحسابات والأدلة» تتكيف مع البنية الدلالية: سرد، مؤشرات رتبية، زيارات متعددة، نتائج متعددة المواد، توزيع مستويات، درجات فعلية، أو بنية عامة.
- أزيلت صياغة «نسبة الإتقان عند تأكيد الدرجة الكلية» من الأنواع التي لا تمثل درجات طلاب، ولا تظهر إلا عندما يقرر الملف الدلالي أن إعدادات الدرجة مطلوبة فعليًا.
- أضيفت تسميات عربية صريحة لعائلات التحليل المعروفة، مع تجاهل المعرفات المستقبلية غير المعروفة وملء الخطة من مسار النوع بدل تكرار «مسار تحليلي».
- لا تغيير في Universal PDF Intake v2 أو الحسابات أو التقارير أو Supabase أو Edge Function.
- أضيفت اختبارات مستقلة تمنع رجوع التكرار أو تسرب منطق الإتقان إلى المؤشرات الإشرافية.


## v1.2.25 — Universal PDF Intake v2

- أضيف `CanonicalDocument 2.0.0` كطبقة بنيوية عامة تسبق التصنيف الدلالي في ملفات PDF.
- تفصل طبقة الإدخال الترويسة والبيانات الوصفية والجداول والأقسام السردية والتذييل والضجيج، مع provenance ودرجة ثقة لكل قرار.
- الجداول لا تُنشأ من الترويسة العامة؛ يجب أن تثبت وجود عناوين أعمدة وصفوف منتظمة قبل تمريرها للتحليل.
- ملف المؤشرات الإشرافية أحادي الصفحة يُطبع كجدول 13×2 من دون قاعدة خاصة بالفيزياء أو بنوع التقرير.
- بقيت مسارات Excel/CSV/Word والتحليل والتقارير وSupabase دون تغيير وظيفي.
- عند عدم اكتشاف جدول موثوق تبقى آلية التوافق القديمة متاحة بدل كسر الملفات السابقة.
- أضيفت اختبارات regression للبنية الرقمية والسردية، واختبار أعمى لجدول dimension/measure غير معروف النوع.


## v1.2.24 — Narrative Adapter Completion

- إضافة مسار PDF سردي متخصص للتقارير الإشرافية التجميعية، مع فصل الأقسام السردية عن الترويسة.
- استخراج المدرسة والمادة والصفوف والعام والتاريخ ورمز المدرسة من PDF السردي وحفظها كبيانات منظمة.
- منع فحوص القيم المفقودة والتكرار الجدولية من التشويش على الوثائق السردية.
- تخصيص شاشة إعداد التحليل السردي بالأدلة البنيوية والسردية بدل المتوسط والوسيط وحد الإتقان.
- تسمية عائلات التحليل السردي صراحةً ومنع ظهور «مسار تحليلي» المكرر.
- ضغط الرسوم القصيرة في التقرير وفق عدد الفئات لزيادة استغلال A4 دون تصغير الخطوط.

# v1.2.24 — Segmented Primary Analysis Pipeline

- استبدال طلب `analyze_primary` الضخم بمقطعين مستقلين: `reasoning` للقراءة التفسيرية و`action` للتدخلات والمتابعة.
- تشغيل المقطعين بالتوازي لتقليل زمن الانتظار، مع مخططي Structured Output أصغر بدل مخطط واحد ثقيل.
- إذا فشل أو نقص مقطع، يُصلح المقطع نفسه فقط؛ لا يعاد التحليل كاملًا ولا يعاد المقطع السليم.
- بعد تركيب المقطعين يظل `validatePrimaryAnalysis` هو الحارس النهائي نفسه، بما في ذلك حماية الأدلة والحسابات ونطاق التدخل.
- أدوات الجودة أصبحت إثرائية وليست شرطًا يسقط التحليل كاملًا إذا لم تكن لازمة.
- مهلة Edge القصوى 45 ثانية، والعميل ينتظر 60 ثانية كنافذة أمان دون تغيير مسار المستخدم.
- Supabase Edge أصبح `V0.15.5`.

# v1.2.22 — Primary Analysis Contract Repair

- إصلاح سبب متكرر لفشل التحليل الأساسي بعد وصول استجابة Gemini سليمة شبكيًا لكنها لا تجتاز عقد الجودة الدلالي.
- مسار الإنقاذ أصبح إصلاحًا موجّهًا: يرسل سبب الرفض والمرشح السابق إلى نموذج إنقاذ أقوى بدل إعادة المحاولة من الصفر.
- نموذج الإصلاح المفضل أصبح `gemini-3.5-flash` مع بقاء `gemini-3.6-flash` و`gemini-3.5-flash-lite` كبدائل.
- إضافة رمز خطأ صريح `GEMINI_CONTRACT_REJECTED` عند فشل الإصلاح بعد المحاولة، ويظل قابلاً لإعادة المحاولة دون فقد الملف أو الأدلة.
- Supabase Edge أصبح `V0.15.4`.

# v1.2.21 — Cross-Section A4 Flow

- يسمح لمحرك A4 ببدء القسم التالي داخل المساحة المتبقية من الصفحة عندما يتسع عنوان القسم وأول كتلة منه بأمان.
- يطبق ضغطًا رأسيًا محدودًا على المسافات والحشوات والحد الأدنى لارتفاع الرسوم فقط عند جسر قسمين، دون تصغير الخطوط أو حذف المحتوى.
- يبقى تقسيم الشبكات عند حدود البطاقات فقط، وتظل البطاقات نفسها غير قابلة للكسر.
- السياسة عامة لكل التقارير ولا تعتمد على نوع الاستمارة أو المحلل.
- لا تغيير في الحسابات أو عقد التحليل أو Supabase Edge.

# v1.2.20 — Resilient AI Recovery

- فصل فشل التحليل الخارجي عن فشل استخراج المحتوى.
- منع فتح نافذة الإدخال اليدوي تلقائيًا عند تعثر القراءة البصرية أو الخدمة.
- الاحتفاظ بالملف داخل الجلسة وإتاحة إعادة القراءة دون رفعه من جديد.
- جعل الإدخال اليدوي خيارًا صريحًا فقط.
- إبقاء الحسابات والأدلة المحلية محفوظة عند فشل التحليل التربوي مع إعادة المحاولة مباشرة.
- إضافة إعادة محاولة شبكية واحدة لطلب التحليل الأساسي عند انقطاع النقل فقط.

# v1.2.19 — Flow Pagination Stabilization

- يعالج الانقسام المفرط الذي ظهر في v1.2.18 عندما كان محرك التدفق يقسم الحاويات القابلة للتجزئة قبل تجربة وضعها كاملة في الصفحة.
- يعتمد استراتيجية whole-first: محاولة الكتلة كاملة أولًا، ثم التجزئة فقط عند الحاجة الفعلية للمساحة.
- يبقي A4 الحقيقي 210 × 297 مم ومحرك الرسوم الدلالي العام دون تغيير.
- يزيل فحص scrollHeight الحساس لتباينات تخطيط Flex ويعتمد الحد الهندسي الفعلي قبل التذييل.

# v1.2.18 — Global A4 Flow Pagination

- استبدال دمج الصفحات الكامل بمحرك تدفق عام يقيس كتل التقرير فعليًا داخل A4.
- يسمح بتقسيم الشبكات العامة عند حدود البطاقات مع إبقاء كل بطاقة كاملة.
- يطبق على جميع أنواع التقارير دون شروط خاصة بنوع استمارة.
- إزالة الارتفاعات الدنيا المصطنعة داخل صفحات التدفق لتقليل الفراغ الأبيض دون تصغير الخط.
- الطباعة التلقائية تنتظر اكتمال الترقيم والتعبئة المقاسة قبل فتح نافذة الطباعة.

## v1.2.17 — True A4 Geometry & Measured Page Packing

- يثبت هندسة التقرير على A4 حقيقي 210 × 297 مم في الشاشة والطباعة.
- يزيل تصغير الصفحة السابق الذي كان يعرض ورقة A4 داخل غلاف أقصر.
- يضيف تعبئة صفحات مقاسة فعليًا في المتصفح: يدمج الأقسام التحليلية المتجاورة فقط عندما تتسع داخل A4 دون تجاوز التذييل، ويتراجع تلقائيًا إذا لم تتسع.
- يلغي تمديد البطاقات الفارغة قسرًا؛ تقليل الهدر أصبح عبر دمج المحتوى الحقيقي لا تضخيم المساحات.
- يبقي فواصل الصفحات والعناصر غير القابلة للكسر وحراس القص كما هي.
- يوضح في شريط التقرير أن المقاس المستهدف A4؛ وقد تتطلب طابعات النظام اختيار A4 من خصائص الطابعة.

## v1.2.16 — Global Visualization Engine

- إضافة `assets/visualization-policy.js` كمحرك مشترك يختار الرسم من النية التحليلية ودلالات البيانات، وليس من نوع التقرير.
- دعم Histogram و100% Stacked وBullet وDumbbell وScatter وPareto المحسن، مع استمرار Bar وLine وRadar وBox وHeatmap وTable.
- تحويل توزيعات المستويات والفئات المحدودة إلى 100% Stacked، وتوزيعات الدرجات إلى Histogram، والحالي/المستهدف إلى Bullet، والقبل/بعد إلى Dumbbell.
- حماية Radar بحد 3–8 أبعاد للقراءة؛ ما زاد يعود تلقائيًا إلى Bar.
- تحديث واجهة النتائج والتقرير الرسمي لاستخدام نفس عقد الرسوم الدلالي.
- تحديث حارس اكتمال الرسوم ليحسب صفوف كل نوع بحسب بنيته بدل افتراض `bar-row`.
- لا تغيير في الحسابات أو الاستنتاجات أو عقد Gemini أو Supabase Edge V0.15.3.

## v1.2.15 — Recognition Authority & Explicit Scale Consent

- تثبيت أولوية عنوان المصدر والبنية المحلية عند تعارض اقتراح التحقق الخارجي مع نوع ملف واضح.
- اعتبار بنية «بنود التقويم + المتوسط + الأكثر تكرارًا» إشارة قوية لمسار فحص أعمال الطلبة.
- حفظ عنوان وسياق صفحة PDF مع الجدول المستخرج بدل فقدهما عند اختيار الجدول.
- منع أي ملف دلالي أو تحقق خارجي من اختيار اتجاه مقياس التقويم نيابة عن المستخدم؛ لا يُقبل الاتجاه إلا إذا كان مؤكدًا صراحةً من المستخدم أو موثقًا في المصدر.
- إبقاء زر الانتقال إلى إعداد التحليل مغلقًا حتى تأكيد اتجاه المقياس أو اختيار التحليل الوصفي فقط.

## v1.2.14 — Scale Semantics Guard

- حذف الأعمدة الوهمية الفارغة التي ينشئها استخراج PDF عندما لا تحمل أي بيانات فعلية.
- إضافة حارس دلالة المقياس لمؤشرات فحص أعمال الطلبة والمؤشرات الإشرافية: لا قوة/ضعف قبل تأكيد اتجاه المقياس.
- دعم ثلاثة أوضاع: القيمة الأقل أفضل، القيمة الأعلى أفضل، أو تحليل وصفي فقط دون أحكام اتجاهية.
- استبعاد صفوف «المستوى الكلي/العام» من قائمة البنود واعتبارها صفوفًا إجمالية لا مؤشرات مستقلة.
- معالجة القيم متعددة المنوال مثل `1,2` كتعدد حقيقي بدل تحويلها خطأ إلى `12`.
- Semantic Profile 2.1.0. لا تغيير في Supabase Edge أو SQL أو الأسرار.

## v1.2.13 — Compact Setup Composition

- أعيد تنظيم شاشة إعداد التحليل إلى عمودين مستقلين بدل صفوف Grid متساوية الارتفاع، لإزالة الفراغات الكبيرة غير المبررة.
- جُمعت «خطة التحليل المقترحة» و«الحسابات والأدلة» في عمود متتابع، مع إبقاء «خدمة التحليل التربوي» في عمود مستقل طبيعي الارتفاع.
- على الشاشات الضيقة تتحول المنطقة إلى عمود واحد من دون فراغات أو تمدد قسري.
- لا تغيير في التحليل أو الحسابات أو التقرير أو Supabase.

## v1.2.12 — Professional Workspace Refinement

- إخفاء الهيرو الكبير بعد بدء سير العمل لتقليل التمرير والتركيز على المهمة الحالية.
- جعل شريط الخطوات ثابتًا أثناء التمرير الطويل.
- معاينة ذكية مختصرة للحقول مع خيار عرض جميع الأعمدة دون تغيير البيانات الأصلية.
- طي تفاصيل فحص الجودة عند سلامة الملف، وفتحها تلقائيًا عند وجود تحذير أو مانع.
- إزالة تفاصيل مزود الخدمة وأرقام Edge من واجهة المستخدم العامة.
- توحيد المصطلحات العامة ومنع ظهور `undefined` و`level distribution` و`semanas` في العرض.
- دمج الحدود والتنبيهات في قسم واحد وإخفاء بطاقة الإجراء المكررة عند وجود خطة تحسين منظمة.
- تحسين تخطيط النتائج، بطاقات التشخيص، خطة المتابعة، وخطة التحسين المتجاوبة دون تمرير أفقي على سطح المكتب.
- إزالة قسم «شفافية التطوير» من واجهة المنتج ونقله ضمنيًا إلى الوثائق.
- لا تغييرات في الحسابات أو الترتيب أو عقد التحليل أو وظيفة Supabase Edge.

# v1.2.5 — Phase 2-D Fix 5

## Executive & Full Reports + Level Provenance Guard

- أضيف زران مستقلان للتقرير التنفيذي المختصر والتقرير الكامل مع الجداول.
- يحتفظ التقرير التنفيذي بصفحات التحليل الجوهرية وترتيب المدرسة أو المادة المختارة، ويحذف ملاحق أوائل المواد غير اللازمة للقرار السريع.
- يبقى التقرير الكامل محافظًا على جميع جداول الأوائل والتعادلات.
- حُصنت دلالة مصدر المستوى: الملفات ذات المستوى المستقل تحتفظ بفحص الاتساق، وملفات الدرجات فقط تعرض أن المستويات مشتقة محليًا وحدود اشتقاقها.
- أضيف حارس مصالحة يمنع Gemini من ادعاء مقارنة درجة ومستوى مستقلين في ملفات المستويات المشتقة.
- لا تغيير في Edge V0.15.2 أو عقد 6.6.0 أو Semantic Profile 2.0.0.

# v1.2.4 — Phase 2-D Fix 4

- دعم ملفات النتائج ذات عمود درجة واحد لكل مادة دون مستوى مستقل.
- اشتقاق المستويات محليًا من الدرجات مع الحفاظ على مصدر المستوى.
- دعم المصنفات متعددة الأوراق واختيار الورقة الشاملة المهيمنة.
- الإبقاء على مسار الدرجة والمستوى الصريح دون تغيير.

# v1.2.3 — Phase 2-D Fix 3

- تحسين قراءة OOXML واكتشاف أزواج الدرجة والمستوى من أي موضع وبالاتجاهين.
- استعادة قائمة المواد من أكثر من مصدر ومنع التنفيذ عند غياب بنية محلية صالحة.
- إضافة بصمات Cache إلى ملفات الواجهة.

# v1.2.2 — Phase 2-D Fix 2

- إضافة مساحة التحليل الشامل أو مادة محددة.
- إضافة أوائل كل مادة وترتيب المدرسة بالمعادلات المعتمدة.
- دعم المراكز المكررة دون كسر التعادل واستبعاد غير مكتمل المواد الأساسية.

# v1.2.1 — Phase 2-D Fix 1

- أضيف مسار نتائج الطلاب الفردية متعددة المواد.
- يعيد قارئ XLSX بناء 13 زوجًا من الدرجة والمستوى من تقارير Crystal ذات العناوين غير المنتظمة.
- لا تظهر إعدادات عمود الدرجة لهذا النوع؛ التحليل يشمل السجل كاملًا.
- أضيفت مقارنة المواد، وتصنيف التعثر متعدد المواد والتخصصي، وفحص اتساق الدرجة والمستوى.
- تحجب الأسماء والجنسية قبل Gemini، ولا تستخدم الجنسية في المقارنة التربوية.
- Edge V0.15.1، مع بقاء Semantic Profile 2.0.0 وعقد التحليل 6.6.0.

# v1.2.0 — Phase 2-D

- إضافة ملف بنية دلالي لكل جدول أو مستند.
- تحقق Gemini الدلالي لكل ملف عند توفر الاتصال.
- توجيه إعدادات الواجهة والمحلل حسب البنية بدل طريقة موحدة.
- دعم كشف توزيع مستويات الأداء المجمع من Crystal Reports بعد حفظه XLSX.
- استبعاد صف الإجمالي من الحساب واستخدامه لفحص الاتساق.
- دعم نوع تكيفي مؤقت للملفات الجديدة غير المسجلة.
- Edge V0.15.0 وSemantic Profile 2.0.0.

# v1.1.5 — Phase 2-C Fix 5

- فحص اتصال Edge حي قبل التحليل.
- عملية `health` لا تستدعي Gemini.
- تطبيع روابط Supabase وCORS وروابط GitHub Pages ذات المسار.
- تعريب أخطاء الشبكة ومنع تسريب `Failed to fetch`.
- لا تغيير في عقد التحليل 6.6.0 أو محرك الزيارات المتعددة.

# v1.1.4 — Phase 2-C Fix 4

## Multi-Visit Sample Scope Guard

- يمنع تعميم تدخلات عينة قسم أو مواد محددة على جميع معلمي المدرسة.
- يبني نطاقًا محليًا من المواد والصفوف وعدد الزيارات والمدرسة.
- يضيق الخادم الأهداف الواسعة إلى الفئة الفعلية، ثم يعيد المتصفح التحقق منها.
- يحافظ على عقد التحليل 6.6.0، ومهلة Gemini، ومحرك الزيارات والرسوم والحارس العددي.

# v1.1.3 — Phase 2-C Fix 3

## Coordinated AI Deadline & Fast Failover

- عالج تعارض مهلة العميل 45 ثانية مع سلسلة المحاولات والنماذج البديلة داخل Edge.
- مهلة كلية للخادم 72 ثانية ونافذة للعميل 85 ثانية.
- محاولة واحدة لكل نموذج ثم انتقال مباشر عند 408/429/5xx أو انقضاء مهلة الاستجابة.
- مهلة 30 ثانية للنموذج الأساسي و24 ثانية لمسار الإنقاذ.
- لا تغيير في عقد التحليل 6.6.0 أو محرك الزيارات أو الرسوم أو الحارس العددي.

# v1.1.2 — Phase 2-C Fix 2

## Transient Gemini Fallback & Arabic Error Shield

- معالجة أخطاء السعة المؤقتة `503/429/408/5xx` بتراجع أُسّي مع jitter.
- التحول التلقائي من Gemini 3.5 Flash إلى 3.6 Flash ثم 3.5 Flash-Lite.
- تعريب رسائل الأعطال وعدم تسريب نص مزود الخدمة الإنجليزي إلى الواجهة.
- إبقاء عقد التحليل 6.6.0 ومحرك الزيارات المتعددة والحسابات والخصوصية دون تغيير.
