# تقارير v0.8.1 - Phase 1-C2 Fix 1

## Chart Completeness Guard

- لم تتغير معادلات بوصلة الإتقان أو فئاتها.
- أصلح التقرير قص الفئات الأخيرة من رسم توزيع الدرجات ورسم فئات التدخل.
- أضيف تخطيط مضغوط مشروط فقط عندما تتجاوز الرسوم الأولية 12 صفًا.
- أزيل استخدام `slice` الصامت في الرسم الشريطي، وأصبح تجاوز 20 فئة خطأً صريحًا يحتاج تقسيم الرسم.
- أضيف تحقق عددي: مجموع فئات `score-histogram` و`intervention-segments` يجب أن يساوي عدد السجلات الصالحة.
- أضيف تحقق بصري داخل المستند يضبط `data-chart-integrity` إلى `pass` أو `fail`.
- اختبرت الصفحة الأولى على A4 وLetter مع ظهور 10 نطاقات و4 فئات كاملة.
- لا تعديل على Supabase أو Gemini أو الأسرار أو محركات التحليل.
