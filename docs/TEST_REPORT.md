# تقرير فحص تقارير v0.9.2

## نطاق الفحص

- منسق التحليل العميق المقسّم.
- عملية Edge الجديدة `enrich_segment`.
- التحقق الخادمي المستقل لكل جزء.
- محاولة `MAX_TOKENS` المختصرة للجزء المتأثر فقط.
- النجاح الجزئي وإعادة الجزء المتعثر فقط.
- المصالحة المرحلية ومنع تغيير الحسابات أو تكرار الخطط.
- معادلات بوصلة الإتقان والرسوم والعائلات التحليلية.

## اختبارات JavaScript

نجحت جميع ملفات `tests/*.test.js`، وتشمل:

- اكتمال الرسوم.
- العائلات التحليلية المتخصصة والمحلل التكيفي.
- عقد Edge القديم والجديد.
- اختبار Edge المقسّم مع محاكاة `MAX_TOKENS` في جزء الاستنتاجات والتأكد أن هذا الجزء وحده أعيد.
- عقد الحمولة والإظهار التدريجي.
- معادلات بوصلة الإتقان وملف الوزارة المرجعي.
- تقليل الحمولة والذاكرة المؤقتة.
- المصالحة ومنع التكرار.
- إنشاء التقرير.
- منسق الأجزاء والنجاح الجزئي.

## اختبار Chromium الفعلي

شُغلت الواجهة في Chromium دون شبكة، مع محاكاة أربعة ردود مستقلة:

1. ظهرت الحسابات والرسوم فورًا.
2. نجحت أجزاء التشخيص والاستنتاجات والتدخلات.
3. فشل جزء الجودة والمتابعة في المحاولة الأولى.
4. ظهرت رسالة `اكتملت مصالحة جزئية آمنة`.
5. بقيت 5 استنتاجات و4 تدخلات و4 مراحل متابعة.
6. ظهر زر `إعادة الأجزاء المتعثرة فقط`.
7. بعد الضغط أعيد جزء الجودة والمتابعة وحده.
8. كانت أعداد الاستدعاءات:
   - التشخيص: 1
   - الاستنتاجات: 1
   - التدخلات: 1
   - الجودة والمتابعة: 2
9. انتهت الواجهة برسالة `اكتملت المصالحة التحليلية المقسّمة`.
10. أخطاء JavaScript: 0.

## فحص TypeScript وJavaScript

- نجح الفحص النحوي لجميع ملفات JavaScript والاختبارات.
- نجح تحويل TypeScript لوظيفة Edge دون أخطاء تشخيصية.
- نجح فحص JSON لملف manifest.

## ملف الدرجات المرجعي

- السجلات الصالحة: 268.
- حققوا حد الإتقان: 48.
- لم يحققوا الإتقان: 220.
- انتشار الإتقان الخام: 17.91044776119403%.
- العرض: 17.9%.
- بقيت 5 استنتاجات و4 تدخلات و4 مراحل متابعة.

## القيد الصريح

لم يُنفذ اتصال حي بمشروع Gemini أو Supabase الخاص بالمستخدم؛ الأسرار غير متاحة في بيئة التجهيز. تم اختبار وظيفة Edge، انقطاع جزء واحد، إعادة الجزء المتعثر، الواجهة، المصالحة، والذاكرة بمحاكاة تنفيذية. الاختبار الحي بعد النشر إلزامي.

## اختبار Overlay وGitHub Pages

- طُبقت حزمة changed-files-only فوق النسخة الكاملة v0.9.1.
- نجحت جميع اختبارات JavaScript واختبار Chromium بعد التطبيق.
- بقيت بصمة `.github/workflows/deploy.yml` دون تغيير:
  `97f6303d141aaa8f4c13f5a7214a78b040baa4c1c5f7de2e0d41dd559550b59b`.
- نجح تشغيل `index.html` عبر HTTP برمز 200.
- تطابق `SUPABASE_FUNCTION_GEMINI_V0_8_0.txt` حرفيًا مع ملف Edge التشغيلي.

## فحص معاينة PDF

- أُنشئت معاينة تقرير من 8 صفحات A4 بعد المصالحة.
- فحص اكتمال الرسوم داخل المستند: `pass`.
- رُندرت الصفحات الثماني إلى PNG وفُحصت بصريًا.
- لا صفحات بيضاء ولا قص ظاهر ولا تجاوز أفقي.
