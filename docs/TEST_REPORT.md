# تقرير فحص تقارير v0.7.2

## سبب الإصلاح
كشف ملف اختبار حي مطبوع عبر Microsoft Print to PDF أن التقرير المصمم بارتفاع A4 كامل تحول إلى 12 صفحة بدل 6، مع صفحة بيضاء أو شبه بيضاء بعد كل صفحة محتوى. أظهر فحص `pdfinfo` أن ملف المستخدم خرج بمقاس Letter لا A4.

## الإصلاح
- إضافة غلاف `report-sheet` بارتفاع آمن 276mm.
- إبقاء لوحة التقرير الداخلية 210x297mm وتحجيمها بنسبة 92.93% داخل الغلاف.
- نقل فاصل الصفحة إلى الغلاف الخارجي.
- دعم A4 وLetter دون تغيير المحتوى أو تقسيم الأقسام.

## الفحوص
- `node --check assets/report-system.js`: PASS
- `node --check assets/app.js`: PASS
- `manifest.json`: PASS
- Overlay فوق v0.7.1 مع بقاء `.github/workflows/deploy.yml`: PASS
- حزمة changed files لا تحتوي `.github` أو `.nojekyll`: PASS
- تقرير الدرجات على A4: 6 صفحات، دون صفحات بيضاء.
- تقرير الدرجات على Letter: 6 صفحات، دون صفحات بيضاء.
- التقرير السردي على A4: 8 صفحات، دون صفحات بيضاء.
- التقرير السردي على Letter: 8 صفحات، دون صفحات بيضاء.
- رندر بصري: لا قص في الترويسة أو التذييل أو البطاقات، ولا صفحة تحتوي بقايا من الصفحة السابقة.

## ملاحظة تشغيلية
يظل اختيار A4 مفضلا للطباعة الرسمية، لكن التصميم لا يتضاعف إذا كان برنامج الطباعة مضبوطا على Letter.
