const fs=require('fs');
const path=require('path');
const assert=require('assert');
const source=fs.readFileSync(path.join(__dirname,'..','supabase','functions','analyze-educational-form','index.ts'),'utf8');
assert.ok(source.includes('DEEP_ANALYSIS_DELTA_SCHEMA'));
assert.ok(source.includes('operation === "enrich"'));
assert.ok(source.includes('validateDeepAnalysisDelta'));
assert.ok(source.includes('callDeepEnrichment'));
assert.ok(source.includes('compactRetryUsed'));
assert.ok(source.includes('candidate.finishReason === "MAX_TOKENS"'));
assert.ok(source.includes('thinkingBudget: 512'));
assert.ok(source.includes('thinkingBudget: 256'));
assert.ok(source.includes('deepAnalysisUnits'));
assert.ok(source.includes('patches'));
assert.ok(source.includes('لا تنشئ تدخلًا أو مرحلة متابعة أو أداة جودة أو استنتاجًا موازيًا'));
assert.ok(source.includes('targetType'));
console.log('PASS edge deep-analysis delta contract, semantic retry and target-field validation');
